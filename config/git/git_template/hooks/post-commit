#!/bin/sh
### lolcommits hook (begin) ###
LANG="en_US.UTF-8" && PATH="/usr/bin:/usr/bin:$PATH" && if [ ! -d "$GIT_DIR/rebase-merge" ] && [ "$LOLCOMMITS_CAPTURE_DISABLED" != "true" ]; then lolcommits --capture --delay 5 --animate 4 --fork; fi
###  lolcommits hook (end)  ###
